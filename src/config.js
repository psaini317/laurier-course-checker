// Courses to watch. termCode is the Banner term code (YYYY + 01 Winter / 05 Spring / 09 Fall).
export const COURSES = [
  { courseCode: "EC260", termCode: "202701", termName: "Winter 2027" },
];

export const STATE_FILE = new URL("../state.json", import.meta.url);
